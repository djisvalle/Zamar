PORT=8787

# Path to a local Audiveris CLI jar (https://github.com/Audiveris/audiveris).
# Required for PDF/photo recognition — see engine/omrEngine.ts. MusicXML
# import works without this set.
AUDIVERIS_JAR_PATH=
